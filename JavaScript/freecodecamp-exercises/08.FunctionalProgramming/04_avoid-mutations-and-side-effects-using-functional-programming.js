// The global variable
var fixedValue = 4;

function incrementer () {
  // Only change code below this line
// Using the increment operator (++) on fixedValue 
// will mutate fixedValue, meaning 
// it will no longer reference the same value
// it was assigned with.

  return fixedValue + 1;

  // Only change code above this line
}